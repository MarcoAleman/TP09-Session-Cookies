<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Practica Integradora</title>
    <link rel="stylesheet" href="style.css">
</head>
<body class="<%= session.color ? session.color : session.user ? session.user.color : ''  %> ">
    <% if (session.user) { %>
        <section>
            <p>Hola <%= session.user.name  %>, elegiste el color <%= session.user.color %>, tu email es <%= session.user.email %> <%= session.user.age !== "" ? `y tu edad es ${session.user.age}.` : ''  %>    </p>
        </section>
    <% } %>

    <form action="/" method="post" id="form-main">
        <div>
            <label for="name">Ingrese su nombre *</label>
            <input type="text" name="name" id="name">
            <% if (locals.errors && errors.name) { %>
                <small class="text-danger"><%= errors.name.msg %></small>
            <% } %>
        </div>
        <div>
            <select name="color">
                <option hidden selected value="">Elija un color *</option>
                <option value="red">Rojo</option>
                <option value="blue">Azul</option>
                <option value="green">Verde</option>
                <option value="yellow">Amarillo</option>
            </select>
            <% if (locals.errors && errors.color) { %>
                <small class="text-danger"><%= errors.color.msg %></small>
            <% } %>
        </div>
        <div>
            <label for="email">Ingrese su email *</label>
            <input type="email" name="email" id="email">
            <% if (locals.errors && errors.email) { %>
                <small class="text-danger"><%= errors.email.msg %></small>
            <% } %>
        </div>
        <div>
            <label for="age">Ingrese su edad</label>
            <input type="number" name="age" id="age">
            <% if (locals.errors && errors.age) { %>
                <small class="text-danger"><%= errors.age.msg %></small>
            <% } %>
        </div>
        <span>Recordar Color <input type="checkbox" name="remember" id="remember"></span>
        <button type="submit">ENVIAR</button>
        <br>
        <div>
            <button type=""><a href="/color"> Olvidar Color</a></button>
        </div>
    </form>
</body>
</html>